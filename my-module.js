const hello = name=>{
    console.log(`Hello ${name}`);
}

const topla = (a,b) => a + b;
const cikar = (a,b) => a - b;

const text = "Text";


const user = {
    name:"Tarlan",
    age: 25,
}


const users = ["Tarlan","Nazim","Namiq"]
export{
    topla,
    cikar,
    text,
    user,
    users
}

export default hello